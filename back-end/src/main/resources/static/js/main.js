/*

var busApi = Vue.resource('/bus{/id}');


Vue.component('bus-row', {
    template: '<div><i>({{}})</i></div>',
    created: function () {
        busApi.get().then(result =>
        console.log(result)
    )

    }
});

var app = new Vue({
    el: '#app',
    data: {
        bus: []
    }
});*/
